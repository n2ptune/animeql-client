query Anime($id: String!) {
  anime(id: $id) {
    id
    type
    attributes {
      canonicalTitle
      synopsis
      favoritesCount
      createdAt
      updatedAt
      coverImage {
        original
      }
      posterImage {
        large
      }
    }
  }
}
